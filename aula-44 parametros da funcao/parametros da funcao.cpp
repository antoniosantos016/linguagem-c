#include <stdio.h>

int main(){
	/* Parâmetros da função:
	permitem ao programar passar 
	uma informação de um trecho de código para dentro da função*/
	float raiz (float r);
	int soma (int x, int y);
	int soma(int x, y);// errado, todos os parâmetros devem ter seu tipo especificado
	float exponencial(float a, int b);
	return 0;
}
